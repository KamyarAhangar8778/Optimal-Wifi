حتما الزامی هستش که ابزار های codebase-memoey-mcp استفاده کنی به صورت متداوم.
کار ها رو موازی همزمان انجام بدی تا سریع تر بریم جلو مثله استفاده موازی از چند ابزار یا خواندن موازی چندین فایل یا ادیت همزمان چند فایل.
داداش ما قراره کتابخانه‌ی معمولی داخل پوشه‌ی: "\lib\WiFi" رو بهینه کنیم و روی اون کار کنیم تا سرعت اون کتابخونه رو بیشتر کنیم با کار هایی که من میگم انجام بدی.
من خودم کتابخانه های کاستوم نوشتم که در پوشه‌ی "\include" هستن و استفاده از اون ها اختیاری هستش.
داداش من یک فایل مهم برای تو نوشتم که داخل اون کاملا توضیح دادم که کتابخانه های من "\include" چه قابلیت هایی دارند و چه گونه میشه از اون ها استفاده کرد:"\include\README.md" این فایل رو حتما وقتی که خواستی از کتابخانه های من استفاده کنی باید بخونی و دیگه فایل های مربوط به کتابخانه های من رو نخونی که کارمون سریع تر پیش بره.
داداش اگه فایل "\include\README.md" رو خوندی و باز هم نتونستی از کتابخانه ها استفاده کنی به درستی یا به هر دلیل دیگه‌ای بهم بگو تا من اجازه بدهم بری فایل های داخل پوشه "\include" رو بخونی.
تو میتونی از همه قابلیت ها و متد ها و هر چیز دیگه‌‌ای که کتابخانه ها من دارند استفاده کنی و محدودتی هم نداری.
تستی که برای کتابخانه‌ی بهینه شده "lib\WiFi" مینویسی باید به گونه‌ای باشد که از پورت سریال استفاده کند تا من خروجی سریال تست رو ببینم.
در بهنیه سازی و ریفکتور فایل ها باید بهم یک حدود درصد واقی از افزایش سرعت و ... داشتیم نسبت به قبل بهینه سازی و ریفکتور ... .
سرعت از از همه چی مهم تر هستش، تو اجازه داری هم حافظه‌ی Ram بی نهایت و هم از حافظه‌ی Flash بی نهایت استفاده کنی و محدودیت نداری.
دستور build:"C:\Users\KAVEH\.platformio\penv\Scripts\platformio.exe run"
همیشه فایل "\ARCHITECTURE.md" رو بخون.
ما باید با یک روشی این کتابخانه رو بهنیه سازی و کاستوم کنیم که من بتونم دقیقا از همین کتابخانه در پروژه ام استفاده کنم به جای ورژن معمولی غیر کاستوم شده که خودمون ساختیم؛ یعنی که نباید به نام و ... API های دیفالت کتابخانه دست بزنیم چون که من از اون ها در پروژه خودم استفاده کردم و اگر آن ها عوض شوند پروژه من به ارور میخوره و کامپایل نمیشه و اون وقت بد بدخت میشم.